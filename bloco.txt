📘 Aula 3 — Introdução a CBF, Model, Templates HTML, ORM

Aula Parte 1️⃣
✅ Class-based Views (CBV)

🎯 Objetivo:

    - Organizar as views de forma mais escalável e reutilizável.
    - Fazer a transição natural entre FBV (function-based views) → que eles já usaram, para CBV → um padrão mais comum em aplicações maiores.

    CBVs oferecem:

    - Mais organização (especialmente para views com vários métodos - GET, POST, PUT, etc.)
    - Facilidade de extensão e herança → permite criar padrões e reaproveitar código.
    - Melhor separação de responsabilidades.

    Quando usar CBV?

    - Em views que têm múltiplos comportamentos.
    - Quando você quer um padrão comum em toda a aplicação.
    - Quando você quer aproveitar views genéricas que o Django já oferece (ListView, DetailView, CreateView, etc).

🛠️ Atividade hands-on (CBV)
👉 Façam:

Criar uma nova rota /server-info que retorna:
    - versão do Python
    - hostname da máquina (dica: usar platform ou socket module)

Exemplo de resposta esperada:
{
  "python_version": "3.11.2",
  "hostname": "meu-computador"
}

Aula Parte 2️⃣
✅ Introdução a Templates HTML
🎯 Objetivo:

    - Usar o sistema de templates do Django.
    - Criar uma pasta de templates.
    - Renderizar páginas HTML dinâmicas.

    1. Templates servem para gerar HTML dinâmico.
        -  Em vez de responder com JsonResponse, agora você responde com uma página HTML.

    2. Django tem uma engine de templates que suporta:
        - Variáveis
        - Loops
        - Condicionais
        - Herança de templates

    3. Estrutura típica de pastas:
        app_name/
        └── templates/
            └── app_name/
                └── arquivo.html
        → Importante: sempre criar a subpasta com o nome do app → boas práticas.


📘 Aula 4 — Integração com Model + Criação e Exibição de Dados via Views

Aula Parte 1️⃣
    1. Orientações sobre a utilização do Git/Github

Aula Parte 2️⃣
🎯 Objetivos da Aula
    - Consolidar o uso de Models e Migrations
    - Implementar inserção e listagem de dados via Django ORM
    - Criar views que exibam dados reais armazenados no banco
    - Estimular boas práticas de design de dados e lógica de consulta

ORM - (Object-Relational Mapping)
    - Permite que você interaja com o banco de dados usando código(da linguagem) em vez de SQL.

Migrations
    - makemigrations - cria o script de alteração
    - migrate - aplica a alteração no banco


📘 Aula 5 — CRUD: Atualização e Remoção de registros

🎯 Objetivos da Aula
    - Compreender os conceitos de atualização(update) e remoção(delete) de dados em uma aplicação web.
    - Implementar views para editar e excluir registros de um model.
    - Trabalhar com rotas dinâmicas baseadas em id.
    - Reforçar o uso de templates, redirecionamentos e organização do código.


Aula Parte 1️⃣
    1. Explicação, implementação da atualização e hands-on


Atividade:
    Seguindo o modelo(Model) Person, implementem as funcionalidades de cadastro e atualização de pessoas.
    Na funcionalidade de edição, incluam uma validação antes do envio dos dados, exibindo a seguinte mnesagem de confirmação: "Deseja continuar?".


Aula Parte 2️⃣
    1. Explicação, implementação da exclusão e hands-on
    2. Refatorar o código usando CBV para views de book e ajuste da rota de book


CRUD:
    Create - insere uma informação na base
    Retrieve - consulta informação(ões) na base
    Update - atualiza uma informação
    Delete - exclui uma informação


rotas book-list x book/list

book-list

- Prós:
 - curta e direta
 - usada quando você considera "book-list" como um único recurso

- Contras
 - Foge um pouco da hierarquia RESTful.
 - Fica menos modular

book/list
- Prós
 - segue estrutura RESTful hierarquica.
    "book" seria uma categoria
    "list" o recurso dentro da categoria
 - Permite expansão futura
  - book/reserva


📘 Aula 6 — Forms: Trabalhando com Django Forms (Formulários dinâmicos e validações)

🎯 Objetivos da Aula
    - Entender o conceito e funcionamento dos formulários do Django
    - Substituir formulários HTML manuais por ModelForm (Hands-on)
    - Beneficios do uso do Django Forms
        - Menos código e mais segurança
        - Valida entradas do usuário de forma declarativa
        - Aprimora a manutenção e reutilização de código em views e templates

Aula Parte 1️⃣
    1. Revisão da aula passada.
    2. Explicação, implementação das métodos(verbos) HTTP não suportados nativamente pelo HTML

    document.getElementById('book-form').addEventListener('submit', function(e) {}

    - Seleciona o formulário pelo ID 'book-form'. ## document.getElementById('book-form')
    - Adiciona um 'ouvinte' de evento ao envio ('submit'). ## addEventListener('submit',
    - Quando o usuário clica em 'Atualizar', a função será executada ## function(e) {

    e.preventDefault();

    - Impede o envio do tradicional do formulário (post)
    - Permite que o JavaScript tome controle e envie os dados com o fetch.

    const form = e.target;
    const data = new FormData(form);
    const jsonData = Object.formEntries(data.entries());

    - e.target é o próprio formulário
    - FormData(form) cria um objeto com todos os campos do formulário
    - Object.formEntries(data.entries()) converte o FormData em um objeto literal JavaScript
    - entries() retorna pare de chaves [chave, valor], e Object.formEntries monta o objeto

    fetch()

    - Envia a requisição HTTP para a URL atual ('window.location.href'), com o método PUT

    method: 'PUT'
    - Define o método de envio como PUT

    Headers

    'Content-Type': 'application/json': indica que o corpo da requisição será em JSON
    'X-CSRFToken': {{ csrf_token }}: necessário para o Django validar o token CSFR

    body: JSON.stringify(jsonData)

    - transforma o objeto em text JSON para envio

    .then(res => res.json())
    .then(data => {
        alert('Atualizado com sucesso!');
        window.location.href = "{% url 'book_list' %}";
    });

    - Espera a resposta do servidor
    - Converte a resposta em JSON
    - Mostra um alerta e redireciona para a listagem


Aula Parte 2️⃣
    1. Explicação, implementação do Django forms

    Existe 2 tipos:
        - forms.Form: formulário genérico sem ligação com o banco de dados
        - forms.ModelForm: formulário baseado em um Model, cria automaticamente


📘 Aula 7 — Forms: Aprofundando Django Forms (Validações, personalizações e formulários manuais)

🎯 Objetivos da Aula
    - Comprrender os conceitos de ModelForm e Form manual
    - Aplicar validações personalizadas em formulários
    - Customizar labels, help_texts e widgets nos campos de formulários

Aula Parte 1️⃣
    1. Revisão da aula passada.
    2. Validações, personizalizações.
    3. Explicação sobre os write case (formas de escrita)

Tipos de escrita (write case)
- Pascal Case (ou Upper Camel Case)
    Primeira letra maiúscula, novas palavras com a primeira letra maiúscula
    Usado por linguagens como: Java, C#, Python (para classes), TypeScript

    Usado para: nomear classes, tipos e enums

    Exemplo:
    - Book
    - HistoryBook
    - RelatorioFinanceiro

- Camel Case
    Primeira letra minúscula, novas palavras com a primeira letra maiúscula
    Usado por linguagens como: Java, JavaScript, C#, TypeScript

    Usado para: nomear atributos, variáveis e métodos

    Exemplo:
    - historyBook
    - Book
    - relatorioFinanceiro

- Snake Case
    Palavras separadas por underline, tudo em minúsculo
    Usado por linguagens como: Python , Ruby

    Usado para: nomear atributos, funções, modulos

    Exemplo:
    - numero_conta
    - history_book
    - published_date

- Constant Case
    Palavras separadas por underline, tudo em maiúsculo.
    Usado por linguagens como: Python, C, C++, Java, C#

    Usado para: nomear constantes (valores imutáveis)

    Exemplo:
    - PI
    - TAXA_JUROS
    - LIMITE_MAXIMO

Aula Parte 2️⃣
    1. hands-on (projeto Gerenciador de tarefas pessoais).
    2. Form manual.


📘 Aula 8 - Banco de dados Relacional - parte 1

🎯 Objetivos da Aula
    - Entender o que é um banco de dados
    - Entender sobre dados estruturados e não estruturados
    - Conceito de tabelas, linhas(registros) e colunas(atributos)
    - Chaves primárias e estrangeiras
    - Relação entre tabelas 1:1, 1:N e N:N
    - Migração SQLite para PostgreSQL

Aula Parte 1️⃣
    1. Entendendo sobre banco de dados

Aula Parte 2️⃣
    1. hands-on configuração e migração da app para o Supabase
    2. hands-on trabalhando com SQL

📘 Aula 9 - Banco de dados Relacional - parte 2

🎯 Objetivos da Aula
    - Entender sobre as versões do uuid
    - Consolidar o entendimento de comandos SQL na prática
    - Relacionar comandos SQL com o uso do ORM do Django
    - (Introdução) Variáveis de ambiente e arquivos .env

Aula Parte 1️⃣
    1. Entendendo sobre banco de dados

Aula Parte 2️⃣
    1. hands-on configuração e migração da app para o Supabase
    2. hands-on trabalhando com SQL

O módulo uuid suporta cinco versões principais de UUIDs.
versão | Nome           | Fonte                                                       | Caso de uso
   1   | Time-based     | Data/hora + MAC do host                                     | Sistemas que precisam de UUIDs ordenáveis pelo tempo. Ex.: Log distribuídos que devem manter a ordem cronológica.
   2   | DCE Security   | Similar ao v1, com suporte UID/GID                          | Controle de acesso embutido do Unix nos UUIDs
   3   | Name-based     | Hash MD5 de uma namespace + nome                            | Quando se precisa do mesmo UUID gerado a partir de um nome fixo. (O mesmo input gera sempre o mesmo UUID)
   4   | Random         | Aleatório (usando um gerador de números aleatórios seguros) | Identificador de transações, objetos, requisições, etc.
   5   | Name-based     | Hash SHA-1 de um namespace + nome                           | URL, DNS, chaves de cache, etc.(O mesmo input gera sempre o mesmo UUID)

DBA <- configura rotinas de backup
backup 10h rodou as 15h01m


caso de uso: a necessidade do sistema/usuário precisa que seja implementado

3 ambientes
dev - ambiente do desenvolvedor (testes)
uat/hml - ambiente de testes do usuário do sistema
prod - ambiente final onde a aplicação irá estar executando (produção)

De/Para SQL x ORM do Django

            SQL                     |       Django ORM
SELECT * FROM public.contacts       |   Contact.objects.all()
WHERE first_name ilike 'Luis'       |   Contact.objects.filter(first_name='Luis')
INSERT INTO                         |   Contact.objects.create(...)
UPDATE                              |   c.fist_name = 'Luis Paulo'; c.save()
DELETE                              |   c.delete()

📘 Aula 10 - Envio de e-mail, Upload de arquivos e Deploy no Render - parte 1

🎯 Objetivos da Aula
    - Configurar envio de e-mails no Django (servidor Gmail)
    - Implementar upload de arquivos localmente
    - Enviar e-mails com arquivo anexado
    - Publicar o projeto na nuvem (Render)

Aula Parte 1️⃣
    1. Envio de e-mail (servidor Gmail)

Aula Parte 2️⃣
    1. Upload de arquivos e envio do path no email
    1. Comparativo upload local x remoto


📘 Aula 11 - Upload de arquivos e Deploy no Render - parte 2

🎯 Objetivos da Aula
    - Implementar upload com serviço remoto (Cloudinary)
    - Publicar o projeto na nuvem (Render)

Aula Parte 1️⃣
    1. Upload de arquivos em serviço remoto
    2. Publicar projeto na nuvem

Aula Parte 2️⃣
    1.